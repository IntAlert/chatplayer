// import { fromJS } from 'immutable';
// import { makeSelectGimpDomain } from '../selectors';

// const selector = makeSelectGimpDomain();

describe('makeSelectGimpDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
